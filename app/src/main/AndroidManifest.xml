<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tb.traineebanking">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.ViewHome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.ViewSplash" />
        <activity android:name=".Activity.ViewLogin" />
        <activity android:name=".Activity.ViewAlterarCadastro" />
        <activity android:name=".Activity.ViewPagamento" />
        <activity android:name=".Activity.ViewEmprestimo" />
        <activity android:name=".Activity.ViewFinanciamento"></activity>
    </application>

</manifest>